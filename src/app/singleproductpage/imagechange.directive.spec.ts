import { ImagechangeDirective } from './imagechange.directive';

describe('ImagechangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagechangeDirective();
    expect(directive).toBeTruthy();
  });
});
